server:
  error:
    include-stacktrace: never

spring:
  jpa:
    hibernate:
      ddl-auto: update #create
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/schoolmanagement-Grp15?createDatabaseIfNotExist=true #default port is 3306
    username: root
    password: